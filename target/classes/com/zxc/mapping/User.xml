<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.zxc.dao.UserDao">  
<!-- 
	<resultMap id="getByTestText" type="TestBean">   
		<id property="id" column="test_id" javaType="string" jdbcType="VARCH AR" />    
		<result property="testText" column="test_text" javaType="string" jdb cType="VARCHAR" />   
	</resultMap> 
 -->

	<select id="getUserByEmailAddress" parameterType="com.zxc.model.User" resultType="com.zxc.model.User">  
	    SELECT * FROM user WHERE emailaddress=#{emailaddress}
	</select>
	<!-- 
	<insert id="addUser" parameterType="com.springMyBatis.system.model.User" flushCache="true">  
	   INSERT INTO user (id,username,password) VALUES (#{id},#{username},#{password})  
	</insert>  
	<update id="updateUser" parameterType="com.springMyBatis.system.model.User">  
	    UPDATE user SET password=#{password} WHERE id=#{id}  
	</update>  
	<delete id="deleteUser" parameterType="int">  
	    DELETE FROM user WHERE id=#{id}  
	</delete>  
	 -->
</mapper>